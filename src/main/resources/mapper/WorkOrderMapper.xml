<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD com.example.Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace=" com.example.demo.mapper.WorkOrderMapper">

    <resultMap id="workOrderMap" type="com.example.demo.model.DO.WorkOrderDO">
        <result property="id" column="id"/>
        <result property="workNo" column="work_no"/>
        <result property="createDate" column="create_date"/>
    </resultMap>

    <select id="ListWorkOrder" resultMap="workOrderMap">
        SELECT * FROM work_order
    </select>

    <!--    <select id="findUserByName" resultMap="result" parameterType="String">
            SELECT * FROM user
            where userName like concat(concat('%',#{userName}),'%')
            order by userId desc
        </select>

        <select id="queryPage" resultMap="result" parameterType="Integer">
            select * from user
            order by userId desc
            limit #{startRows},5
        </select>

        <select id="getRowCount" resultType="Integer">
            select count(*) from user
        </select>-->

    <insert id="insertWorkOrder" useGeneratedKeys="true" keyProperty="id"
            parameterType ="com.example.demo.model.DO.WorkOrderDO">
        INSERT INTO work_order
        (
        work_no,create_date
        )
        VALUES (
        #{workNo},
        now()
        )
    </insert>

   <!-- <delete id="delete" parameterType="int">
        delete from user where userId=#{userId}
    </delete>

    <update id="Update" parameterType="com.example.demo.entity.User">
        update user
        set user.userDate=#{userDate},user.userName=#{userName},user.userAddress=#{userAddress}
        where user.userId=#{userId}
    </update>-->
</mapper>